spring:
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    password: guest
    username: guest
    listener:
      type: simple
      simple:
        default-requeue-rejected: false
        acknowledge-mode: manual
server:
  port: 8080